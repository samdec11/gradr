.row
  #all_classes
    - @groups.each do |group|
      %li.class_names
        = group.name
        %ul.class_students.hide
          Students
          - group.students.order(:name).each do |student|
            %li= link_to student.name, student
